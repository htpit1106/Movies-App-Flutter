import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors{
  AppColors._();
  static const Color background = Color(0xFF242A32);

  // text
  static const Color textOrange = Color(0xFFFF8700);
  static const Color textWhite = Colors.white;
  static const Color textGray = Color(0xFF92929D);

  // nav bar
  static const Color navbarColor = Color(0xFF242A32);
  // divider
  static const Color dividerColor = Color(0xFF696974);
}